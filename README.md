# GitHub Manager

Менеджер репозиториев GitHub - Веб и Electron приложение для управления GitHub репозиториями.

## Структура проекта

```
github-manager/
├── src/                    # Исходный код
│   ├── styles/            # CSS модули
│   │   ├── variables.css  # CSS переменные
│   │   ├── base.css       # Базовые стили
│   │   ├── animations.css # Анимации
│   │   ├── components.css # Компоненты
│   │   ├── layout.css     # Layout стили
│   │   └── main.css       # Главный файл стилей
│   ├── js/                # JavaScript модули
│   │   ├── api/           # API клиенты
│   │   │   └── github.js  # GitHub API
│   │   ├── modal/         # Модальные окна
│   │   │   └── modal.js   # Modal Manager
│   │   ├── ui/            # UI компоненты
│   │   │   ├── tabs.js    # Tab Manager
│   │   │   └── dropdown.js # Dropdown Manager
│   │   ├── editor/        # Редактор
│   │   │   └── fileEditor.js # File Editor
│   │   ├── chat/          # Чат
│   │   │   └── chat.js    # Chat Manager
│   │   ├── utils/         # Утилиты
│   │   │   └── auth.js    # Auth Manager
│   │   └── main.js        # Точка входа
│   └── index.html         # Главная HTML страница
├── electron/              # Electron конфигурация
│   ├── main.js           # Main процесс
│   └── preload.js        # Preload скрипт
├── public/               # Публичные файлы
├── dist/                 # Собранные файлы (создается автоматически)
├── package.json          # Зависимости и скрипты
├── vite.config.js        # Конфигурация Vite
└── README.md             # Документация
```

## Установка

### 1. Установите Node.js

Убедитесь, что у вас установлен Node.js версии 18 или выше:

```bash
node --version
```

Если Node.js не установлен, скачайте его с [nodejs.org](https://nodejs.org/)

### 2. Установите зависимости

```bash
npm install
```

## Запуск

### Веб-версия (для разработки)

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Просмотр собранной версии

```bash
npm run preview
```

### Electron версия (для разработки)

```bash
npm run electron:dev
```

Это запустит Vite dev сервер и Electron приложение одновременно.

### Сборка Electron приложения

```bash
npm run electron:build
```

Это создаст установочные файлы для вашей платформы в папке `dist-electron/`

### Упаковка Electron приложения (без установщика)

```bash
npm run electron:pack
```

## Использование

### Веб-версия

1. Откройте браузер и перейдите на http://localhost:5173
2. Используйте интерфейс для управления репозиториями

### Electron версия

1. Запустите `npm run electron:dev`
2. Приложение откроется в отдельном окне

## Функциональность

- 📁 Управление репозиториями
- 🌿 Работа с ветками
- 🔀 Pull Requests
- ✏️ Встроенный редактор кода
- 💬 Чат-помощник
- 🔐 Аутентификация
- 📊 Аналитика и статистика

## GitHub API

Для работы с GitHub API вам понадобится токен доступа:

1. Перейдите в GitHub Settings > Developer settings > Personal access tokens
2. Создайте новый токен с необходимыми правами
3. Введите токен в настройках приложения

## Разработка

### Структура модулей

- **API модули**: Взаимодействие с внешними API (GitHub)
- **UI модули**: Управление интерфейсом (табы, дропдауны, модальные окна)
- **Editor модули**: Редактор файлов
- **Chat модули**: Чат функциональность
- **Utils модули**: Вспомогательные функции

### Добавление новых функций

1. Создайте новый модуль в соответствующей папке
2. Импортируйте его в `src/js/main.js`
3. Используйте в приложении

## Лицензия

MIT

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.
