# 🔧 Решение проблем с GitHub токеном

## Проблема: Токен не принимается

### 🔍 Шаг 1: Проверка токена

#### 1.1 Проверьте формат
Правильный токен выглядит так:
```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
- Начинается с `ghp_`
- Длина: 40-50 символов
- Без пробелов в начале/конце

#### 1.2 Проверьте срок действия
1. Откройте: https://github.com/settings/tokens
2. Найдите ваш токен `GITODY-...`
3. Проверьте статус:
   - ✅ **Active** - работает
   - ❌ **Expired** - создайте новый

#### 1.3 Проверьте права (Scopes)
Необходимые права:
- ✅ `repo` (все подпункты)
- ✅ `read:user`
- ✅ `user:email` (опционально)

### 🔨 Шаг 2: Создать новый токен

#### Способ 1: Через GiTody (автоматически)
1. Откройте GiTody
2. Нажмите **"Создать токен"** или **"Получить токен"**
3. Откроется GitHub с готовыми настройками
4. Просто нажмите **"Generate token"** внизу страницы
5. Скопируйте токен (показывается только один раз!)
6. Вставьте в GiTody

#### Способ 2: Вручную
1. Откройте: https://github.com/settings/tokens/new
2. **Note**: `GiTody` (любое имя)
3. **Expiration**: выберите срок (30/60/90 days или No expiration)
4. **Select scopes**:
   - ☑️ `repo` (все галочки ниже)
   - ☑️ `read:user`
   - ☑️ `user:email`
5. Нажмите **"Generate token"** внизу
6. Скопируйте токен (показывается только один раз!)

### 📊 Шаг 3: Диагностика

#### Откройте консоль браузера
1. Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
2. Перейдите на вкладку **Console**
3. Введите токен и нажмите **"Подключить"**
4. Смотрите логи - они покажут точную проблему:

```
[Auth] === НАЧАЛО ПОДКЛЮЧЕНИЯ ===
[Auth] Токен: ghp_xxxxxxxx... (длина: 40)
[Auth] Отправка запроса к GitHub API...
[Auth] Ответ: 200 OK
[Auth] Права: ["repo", "read:user"]
[Auth] Пользователь: YourUsername
[Auth] === ✓ ПОДКЛЮЧЕНИЕ УСПЕШНО ===
```

### ❌ Распространённые ошибки

#### Ошибка 401: "Неверный токен"
**Причины:**
- Токен скопирован не полностью
- Токен удалён на GitHub
- Токен просрочен
- Лишние пробелы в начале/конце

**Решение:** Создайте новый токен

#### Ошибка 403: "Доступ запрещён"
**Причины:**
- Не хватает прав (scopes)
- Превышен лимит запросов

**Решение:** Создайте токен с правами `repo` и `read:user`

#### Ошибка "Недостаточно прав"
**Причина:** Токен создан без `repo` scope

**Решение:**
1. Удалите старый токен
2. Создайте новый с галочкой `repo`

### 🔒 Безопасность

⚠️ **Не делитесь токеном!**
- Не отправляйте в чаты/email
- Не коммитьте в Git
- Не загружайте на скриншоты

✅ **Безопасно:**
- Хранится только в вашем браузере (localStorage)
- Можно удалить в любой момент
- Можно создать новый в любой момент

### 👨‍💻 Для разработчиков

#### Проверить токен вручную (curl)
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     -H "Accept: application/vnd.github+json" \
     https://api.github.com/user
```

#### Очистить localStorage
В консоли браузера:
```javascript
localStorage.clear();
location.reload();
```

#### Проверить сохранённый токен
```javascript
session.debug();
```

### 🆘 Нужна помощь?

Если проблема остаётся:
1. Откройте issue: https://github.com/Lobcter2dy/GiTody/issues
2. Приложите логи из консоли (без токена!)
3. Опишите точную ошибку
